# ─── Kronos AIOps — Production Environment Variables ─────────────────────────
#
# These values are baked into the Vite bundle at build time.
# They are NOT secrets — they get embedded in the public JS bundle anyway.
# Vercel's GitHub integration reads this file automatically on every deploy.
#
# To override locally, use .env.local (gitignored).
# ─────────────────────────────────────────────────────────────────────────────

VITE_API_BASE_URL=https://tozg3pwytc.execute-api.us-east-1.amazonaws.com
VITE_WS_URL=wss://r0o037d2gc.execute-api.us-east-1.amazonaws.com/prod
VITE_USE_MOCK=false

# Cognito Auth
VITE_COGNITO_USER_POOL_ID=us-east-1_b7LI08o2i
VITE_COGNITO_CLIENT_ID=7de4o5p8lp0u4tgvp4sscm2e02
VITE_COGNITO_DOMAIN=https://aiops-auth-807430513014.auth.us-east-1.amazoncognito.com
